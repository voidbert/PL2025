program io;

type
    enum = (red, green, blue);
var
    a : integer;
    b : boolean;
    c : char;
    d : real;
    e : string;
    f : enum;
begin
    read(a);
    writeln(a);

    read(b);
    writeln(b);

    read(c);
    writeln(c);

    read(d);
    writeln(d);

    read(e);
    writeln(e);

    read(f);
    writeln(f)
end.
